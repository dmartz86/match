.row
  .col-md-12(ng-show='done')
    .well
      p.announcement-heading(style='font-size: 20px;') Well done {{user.name || 'user' }}! Your next level is {{level}}. The highest level is {{MAX_LEVEL}}.